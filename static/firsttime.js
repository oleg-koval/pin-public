// Generated by CoffeeScript 1.4.0
(function() {
  var $firstTime, $notice, counter, messages, showNext;

  messages = ["Let's get you started! Under this box, we have Pins.", "You can add these items to your Getlist.", "Doing so is easy. Just click the thumbtack button in the top-left of any Pin.", "Try adding five items to your Getlist!"];

  counter = 0;

  $notice = $('.notice');

  $firstTime = $('.first-time');

  showNext = function() {
    $notice.text(messages[counter]);
    counter++;
    if (counter === messages.length) {
      $('#next').text('Finish');
      return $('#skip').hide();
    } else if (counter > messages.length) {
      return $firstTime.hide();
    }
  };

  $('#skip').click(function() {
    return $firstTime.hide();
  });

  $('#next').click(function() {
    return showNext();
  });

  showNext();

}).call(this);
